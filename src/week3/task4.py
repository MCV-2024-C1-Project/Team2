# For QSD2-W3 : detect all the paintings (max 2 per image), remove background,
# apply retrieval system, return correspondences.
#   - Add extra list level to allow for the possibility of more than one painting per image
